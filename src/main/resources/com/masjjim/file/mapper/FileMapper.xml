<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.masjjim.file.mapper.FileMapper">

    <insert id="createFile" useGeneratedKeys="true" keyProperty="fileNo">
        INSERT INTO T_400
      			( file_path
      			, file_type
      			, file_name
      			, file_tr_name
      			, file_code
      			, file_rel1
      			, file_rel2
      			, file_reg_dt
      			)
      		VALUES
      			( #{filePath}
      			, #{fileType}
      			, #{fileName}
      			, #{fileTrName}
      			, #{fileCode}
      			, #{fileRel1}
      			, #{fileRel2}
      			, now()
      			)
    </insert>

</mapper>